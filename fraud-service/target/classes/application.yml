server:
  port: 8081
spring:
  application:
    name: fraud-service
  datasource:
    password: "weone123"
    url: jdbc:postgresql://localhost:5432/fraud
    username: "weone"
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true